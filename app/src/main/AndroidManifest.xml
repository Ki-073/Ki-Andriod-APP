<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 开机自动越狱：接收开机广播 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- 开机自动越狱：兜底通知（Android 13+） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <queries>
        <package android:name="com.sukisu.ultra" />
        <package android:name="me.weishu.kernelsu.pr" />
        <package android:name="me.weishu.kernelsu" />
        <package android:name="com.resukisu.resukisu" />
        <package android:name="com.kowx712.supermanager" />
        <!--
          ⚠ Android 11+ 包可见性：不声明的话 resolveActivity(tgIntent) 会返回 null，
          导致「加入频道」跳过 tg:// 分支、直接回退到网页。
          这里把 Telegram 各分支包名 + 通用 web 意图都声明进来。
        -->
        <package android:name="org.telegram.messenger" />
        <package android:name="org.telegram.messenger.web" />
        <package android:name="org.telegram.csc.messenger" />
        <package android:name="org.telegram.plus" />
        <package android:name="tw.nekomimi.nekogram" />
        <package android:name="nekox.messenger" />
        <!-- QQ（问题反馈跳转） -->
        <package android:name="com.tencent.mobileqq" />
        <package android:name="com.tencent.tim" />
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tg" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mqqwpa" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="mqqapi" />
        </intent>
    </queries>
    <application
        android:name=".GhostlockApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GhostLock">
        <activity
            android:name=".ui.MainActivity"
            android:launchMode="singleTop"
            android:excludeFromRecents="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|mnc|colorMode|density|fontScale|fontWeightAdjustment|keyboard|layoutDirection|locale|mcc|navigation|smallestScreenSize|touchscreen|uiMode"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>